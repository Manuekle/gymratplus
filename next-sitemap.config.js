/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: "https://gymratplus.vercel.app", // 🔴 Reemplaza con tu dominio
  generateRobotsTxt: true, // Genera automáticamente robots.txt
  sitemapSize: 5000, // Cantidad de URLs por sitemap
};

module.exports = config;
